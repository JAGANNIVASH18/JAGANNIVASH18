name: Generate Trophy

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Generate Trophy
        uses: ryo-ma/github-profile-trophy-action@master
        with:
          username: JAGANNIVASH18
